

hydra:
  searchpath:
    - ${oc.env:HM3D_DATA_COLLECTOR_CFG_DIR}
    
defaults:
  - general
  - habitat_sim
  - data_collection
  - process_gt
  - _self_

data_collection:
  init_pos: [0.0, 0.0, 0.0]

hm_split: train
hm_idx_scene: '00020'
hm_scene_name: XYyR54sxe6b 
hm_data_version: 0

data_dir: ~/hm_data_collected/
hm3d_dir: /media/datasets/habitat/v0.2/
log_dir: ${get_hydra_file_dirname:}/logs/${script}/${date}

